# Silence ggplot warnings

Replacement for `print.ggplot()` that mutes warnings.

## Usage

``` r
ggplot_silent(plot_obj, ...)
```

## Arguments

- plot_obj:

  input

- ...:

  Arguments are passed to `print.ggplot()`
