# plot draws

Function to plot spaghetti draws from time-to-competency brms model

## Usage

``` r
plot_draws(input, slice = 100)
```

## Arguments

- input:

  output from return_draws()

- slice:

  number of simulations to draw for spaghetti plot (default 100)
