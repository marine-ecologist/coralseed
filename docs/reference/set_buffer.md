# Set buffer

Function to generate an sf file for bounding box. Used only for tmap
visualisations

## Usage

``` r
set_buffer(input = NULL, buffer = 150)
```

## Arguments

- input:

  input (defaults to NULL)

- buffer:

  distance surrounding particle boundary (defaults to 250m)
