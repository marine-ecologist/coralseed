% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_vignettes.R
\name{render_vignettes}
\alias{render_vignettes}
\title{Render package Quarto vignettes one by one}
\usage{
render_vignettes(
  path = "vignettes",
  pattern = "\\\\.qmd$",
  files = NULL,
  quiet = FALSE,
  output_dir = NULL,
  stop_on_error = FALSE
)
}
\arguments{
\item{path}{Character. Directory containing Quarto vignette files. Default is
\code{"vignettes"}.}

\item{pattern}{Character. Regular expression used to identify Quarto files.
Default is \code{"\\\\.qmd$"}.}

\item{files}{Optional character vector of files to render. If supplied,
\code{path} and \code{pattern} are ignored.}

\item{quiet}{Logical. Passed to \code{\link[quarto:quarto_render]{quarto::quarto_render()}}. Default is
\code{FALSE} so that full render output is printed.}

\item{output_dir}{Optional output directory passed to
\code{\link[quarto:quarto_render]{quarto::quarto_render()}}. Default is \code{NULL}, which lets Quarto use its
standard output location.}

\item{stop_on_error}{Logical. If \code{TRUE}, stop after the first failed render.
If \code{FALSE}, continue rendering all files and return a results table.
Default is \code{FALSE}.}
}
\value{
A tibble with columns:
\describe{
\item{file}{Path to the rendered \code{.qmd} file.}
\item{status}{Either \code{"ok"} or \code{"failed"}.}
\item{error}{Error message if rendering failed, otherwise \code{NA}.}
}
}
\description{
Renders \code{.qmd} files in a package vignette directory and returns a summary
table showing which files rendered successfully and which failed. This is
useful for diagnosing Quarto/pkgdown failures because each vignette is
rendered separately and errors are captured rather than stopping the whole
build.
}
\examples{
\dontrun{
render_vignettes()

render_vignettes(
  path = "vignettes",
  quiet = FALSE,
  stop_on_error = FALSE
)

render_vignettes(
  files = c(
    "vignettes/scenario_lizard.qmd",
    "vignettes/01_lizard_end_to_end.qmd"
  )
)
}

}
