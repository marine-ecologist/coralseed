% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_particles.R
\name{simulate_particles}
\alias{simulate_particles}
\title{Simulate Lagrangian particle trajectories from gridded flow fields}
\usage{
simulate_particles(
  flow_field,
  land_poly = NULL,
  release_sites,
  crs_m = 32755,
  dt = 5 * 60,
  release_duration_seconds = 60 * 60,
  release_by = paste(dt, "sec"),
  release_start = NULL,
  n_particles_per_site_per_release = 100,
  K = 0.05,
  max_land_retry = 200,
  seed = 101,
  parallel = FALSE,
  workers = NULL
)
}
\arguments{
\item{flow_field}{An \code{sf} point object containing gridded flow fields. Must
contain columns \code{time}, \code{x}, \code{y}, \code{u_ms}, and \code{v_ms}. Velocities should be
in metres per second.}

\item{land_poly}{An \code{sf} polygon object representing land or other exclusion
areas that particles should not enter. Use \code{NULL} to disable land
exclusion.}

\item{release_sites}{An \code{sf} point object giving particle release locations.}

\item{crs_m}{Numeric or character. Projected coordinate reference system
used for particle movement in metres. Default is \code{32755}.}

\item{dt}{Numeric. Particle advection time step in seconds. Default is
\code{5 * 60}.}

\item{release_duration_seconds}{Numeric. Duration over which particles are
released, in seconds. Default is \code{60 * 60}.}

\item{release_by}{Character. Interval between release times, passed to
\code{seq.POSIXt()}. Default is \code{paste(dt, "sec")}, so release times align with
flow-field time steps.}

\item{release_start}{Optional release start time. If \code{NULL}, the first
available flow-field timestamp is used.}

\item{n_particles_per_site_per_release}{Integer. Number of particles released
per site at each release time. Default is \code{100}.}

\item{K}{Numeric. Horizontal diffusivity used in the random-walk term, in
square metres per second. Default is \code{0.05}.}

\item{max_land_retry}{Integer. Maximum number of redraw attempts for
particles that intersect land after movement. Default is \code{200}.}

\item{seed}{Integer. Random seed for reproducible particle release and
diffusion. Default is \code{101}.}

\item{parallel}{Logical. If \code{TRUE}, run release sites in parallel using
\code{future} and \code{furrr}. Default is \code{FALSE}.}

\item{workers}{Integer. Number of parallel workers. If \code{NULL}, uses one fewer
than the number of available cores.}
}
\value{
A named list with:
\describe{
\item{particle_tracks}{A data frame containing all particle positions
through time.}
\item{particle_tracks_sf}{An \code{sf} point object of all particle positions in
\code{crs_m}.}
\item{particle_tracks_4326}{An \code{sf} point object of all particle positions
transformed to EPSG:4326.}
\item{final_particles}{An \code{sf} point object containing the final recorded
position of each particle in \code{crs_m}.}
\item{final_particles_4326}{Final particle positions transformed to
EPSG:4326.}
\item{particle_track_summary}{A one-row summary table of particle counts,
track rows, and land-retry diagnostics.}
\item{particles0}{Initial particle release table.}
\item{release_sites_m}{Release sites transformed to \code{crs_m}, with release
coordinates.}
\item{land_m}{Land polygon transformed to \code{crs_m}, or \code{NULL} if
\code{land_poly = NULL}.}
}
}
\description{
Simulates passive particle dispersal from one or more release sites using
gridded eastward and northward velocity fields. Particles are released over
a specified release window, advected using the nearest available flow-field
grid cell, and moved with an added random-walk diffusion term. Candidate
particle positions that intersect land are repeatedly redrawn up to a
maximum retry limit.
}
\details{
The time loop is sequential because each particle position depends on its
previous position. Optional parallel processing is therefore implemented by
splitting independent simulations across release sites.

Particle movement follows:

\deqn{x_{t + 1} = x_t + u_t dt + \epsilon_x}

\deqn{y_{t + 1} = y_t + v_t dt + \epsilon_y}

where \code{u_t} and \code{v_t} are the nearest-neighbour flow velocities and
\eqn{\epsilon_x} and \eqn{\epsilon_y} are normally distributed random-walk
terms with standard deviation:

\deqn{\sqrt{2 K dt}}

Particles that intersect \code{land_poly} after movement are redrawn from the
previous position using the same advective and diffusive step. If a particle
still intersects land after \code{max_land_retry} attempts, it is returned to its
previous position for that time step.

Flow fields are matched exactly by timestamp. For this reason,
\code{release_by = paste(dt, "sec")} is recommended unless the flow field has
finer temporal resolution.
}
\examples{
\dontrun{
particle_sim <- simulate_particles(
  flow_field = flow_sim$flow_field,
  land_poly = land_poly,
  release_sites = release_sites,
  crs_m = 32755,
  dt = 5 * 60,
  release_duration_seconds = 60 * 60,
  n_particles_per_site_per_release = 100,
  K = 0.05,
  max_land_retry = 200,
  seed = 101,
  parallel = TRUE,
  workers = 6
)

particle_sim$particle_tracks_sf
particle_sim$final_particles_4326
particle_sim$particle_track_summary
}

}
